<nav class="navbar navbar-expand-lg bg-white p-0">
  <div class="container-fluid">
    <span class="navbar-brand">
      <%= link_to root_path, data: { turbo: false } do %>
        <%= image_tag "title-lemontech-color.svg", alt: "LemontechEvents", class: "d-inline-block align-text-top" %>
      <% end %>
    </span>
    <ul class="navbar-nav ms-auto d-flex align-items-center">
      <li class="nav-item d-flex align-items-center">
        <% if params[:all] %>
          <%= link_to events_path, class: "nav-link" do %>
            <span>Mis Eventos</span>
          <% end %>
        <% else %>
          <%= link_to events_path(all: true), class: "nav-link" do %>
            <span>Eventos</span>
          <% end %>
        <% end %>
      </li>
      <li class="nav-item d-flex align-items-center">
        <%= link_to edit_user_registration_path, class: "nav-link d-flex align-items-center profile-link", data: { turbo: false } do %>
          <%= image_tag "profile-blank.png", alt: "Perfil", class: "rounded-circle", width: 30, height: 30 %>
          <span>Perfil</span>
        <% end %>
      </li>
      <li class="nav-item">
        <%= link_to 'Cerrar Sesión', destroy_user_session_path, data: { turbo: false }, class: "nav-link logout-link" %>
      </li>
    </ul>
  </div>
</nav>

<div class="container-fluid">
  <main class="main-content p-4 pt-0 pb-0">
    <%= render 'layouts/flash_messages' %>
    <%= yield %>
  </main>
</div>
